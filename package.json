{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tailwindcss -i ./source/app.css -o ./styles.css -w -m"
  },
  "devDependencies": {
    "tailwindcss": "^3.1.4"
  }
}
